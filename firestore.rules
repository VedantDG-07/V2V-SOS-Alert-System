rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development mode (replace with production block below before launch)
    match /{document=**} {
      allow read, write: if true;
    }

    // Production starter rules (reference)
    // match /active_users/{userId} {
    //   allow read: if true;
    //   allow write: if request.auth != null && request.auth.uid == userId;
    // }
    // match /accident_events/{eventId} {
    //   allow read: if true;
    //   allow create: if request.auth != null;
    //   allow update, delete: if false;
    // }
    // match /emergency_alerts/{alertId} {
    //   allow read: if request.auth != null && request.auth.token.role == 'service';
    //   allow create: if request.auth != null;
    // }
    // match /user_alerts/{userId}/alerts/{alertId} {
    //   allow read: if request.auth != null && request.auth.uid == userId;
    //   allow create: if request.auth != null;
    //   allow delete: if request.auth != null && request.auth.uid == userId;
    // }
  }
}
